name: Flutter Android Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: 'temurin', java-version: '17' }
      - uses: subosito/flutter-action@v2
        with: { channel: 'stable', cache: true }
      - run: flutter pub get
      
      # Ez a parancs javítja a v1/v2 ütközést és létrehozza az internet-engedélyt tartalmazó Android mappát
      - run: flutter create . --platforms=android

      - run: flutter build apk --release --dart-define=SPORTSDB_API_KEY=${{ secrets.SPORTSDB_API_KEY }}
      
      - uses: actions/upload-artifact@v4
        with:
          name: Zsolt-AI-PRO-APK
          path: build/app/outputs/flutter-apk/app-release.apk
